#▄────────────────────────────────▄
#█                                █
#█  Controller: Run               █
#█  • Запуск команд (управление)  █
#█                                █
#▀────────────────────────────────▀
controller:Run() { case "$1" in
# Общие команды
    'run_old') model:Run "$@" ;; # Проверяет запущен-ли старый контейнер
    'run')     model:Run "$@" ;; # Проверяет запущен-ли контейнер
    'no_cd')   model:Run "$@" ;; # Проверяет задана-ли переменная '$CD'
esac
}
